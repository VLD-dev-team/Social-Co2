import 'package:flutter/material.dart';

class CallToPost extends StatelessWidget {
  const CallToPost({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('call to post');
  }
}
